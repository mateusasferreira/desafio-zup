import styled from "styled-components";

export const Wrapper = styled.section`
    max-width: 1240px;

    display: flex;
    justify-content: space-around;

    margin: 0 auto;

    padding: 60px 0;
`